<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Botón primario TrainUP: uso general en toda la app (mockup) -->
    <style name="TrainUp.Button.Primary" parent="Widget.Material3.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:insetTop">12dp</item>
        <item name="android:insetBottom">12dp</item>
        <item name="android:elevation">2dp</item>
        <item name="rippleColor">?attr/colorPrimaryContainer</item>
    </style>
</resources>

